import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Fridgey iOS — pendiente de implementar")
            .padding()
            .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .center)
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
